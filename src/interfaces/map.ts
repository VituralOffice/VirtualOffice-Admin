export interface IMap {
  _id: string;
  name: string;
  capacity: number;
  totalMeeting: number;
  totalChair: number;
  totalWhiteboard: number;
  default: boolean;
  style: string;
  icon: string;
  json: string;
  createdAt: string;
  active: boolean;
}
